package com.team3web.shop.controller;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

@Controller
public class PopupController {

	/*
	 * @GetMapping("showBuyPopup") public String showBuyPopup(@RequestParam String
	 * item, @RequestParam String immediatly ,
	 * 
	 * @RequestParam String actionBoardNo, Model model){
	 * 
	 * model.addAttribute("actionBoardItemName",item);
	 * model.addAttribute("immediatly",immediatly);
	 * model.addAttribute("actionBoardNo",actionBoardNo);
	 * model.addAttribute("name",principal.getName());
	 * model.addAttribute("email",principal.getEmail());
	 * model.addAttribute("phone",principal.getPhone()); return "payment/popup"; }
	 */
	
}
