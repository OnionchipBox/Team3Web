<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.team3web.shop.mapper.SellerMapper">
	
	<!-- 저장하기 : 판매자 등록 -->
	<insert id="insertSeller" parameterType="SellerVO">
   	insert into shop_seller
      	(ID, SELLER_NAME, SELLER_REGISTRATION_NUMBER, REGISTRATION_DATE)
   	values 
      	(#{id}, #{seller_name}, #{seller_registration_number}, to_char(sysdate, 'YYYYMMDD HH24:MI:SS'))
	</insert>
	
	<!-- 등급 변경(판매자) -->
	<update id="updateSellerVerify" parameterType="UserVO">
    	UPDATE shop_user
    	SET VERIFY = 'SELLER'
    	WHERE ID = #{id}
	</update>
	
	<!-- 아이디 조회 -->
	<select id="checkSellerId" parameterType="string" resultType="SellerVO">
    	SELECT *
    	FROM shop_seller
    	WHERE id = #{id}
	</select>

</mapper>