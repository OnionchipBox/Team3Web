<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team3web.shop.mapper.AdminGongjiMapper">


	<!-- 공지사항 저장 -->
	<insert id="in_gongji" parameterType="GongjiVO">
		insert into
		shop_gongji(gno,gname,gtitle,gpwd,gcont,ghit,gdate)
		values(gongji_no_seq.nextval,#{gname},#{gtitle},#{gpwd},#{gcont},0,sysdate)
	</insert>


<!-- 검색 전후 레코드 개수 -->
	<select id="gongji_count" resultType="int">
		select count(gno) from shop_gongji
		<where>
			<if test="find_field == ''">
				<!-- 검색조건이 없어서 총레코드 개수 -->
			</if>
			<if test="find_field == 'gtitle'">
				<!-- 검색 조건이 제목인 경우 -->
				gtitle like #{find_name} <!-- like는 오라클 검색 연산자 -->
			</if>
			<if test="find_field == 'gcont'"><!-- 검색 조건이 공지 내용인 경우 -->
				gcont like #{find_name}
			</if>
		</where>
	</select>


<!-- 검색전후 공지목록 -->
	<select id="gongji_list" resultType="GongjiVO">
		select * from
		(select rowNum
		rNum,gno,gname,gtitle,ghit,gdate
		from
		(select * from shop_gongji
		<where>
			<if test="find_field == ''"> <!-- 검색조건이 없어서 총레코드 개수 -->
			</if>
			<if test="find_field == 'gtitle'"><!-- 검색 조건이 제목인 경우 -->
				gtitle like #{find_name} <!-- like는 오라클 검색 연산자 -->
			</if>
			<if test="find_field == 'gcont'"> <!-- 관리자 공지 내용인 경우 -->
				gcont like #{find_name}
			</if>
		</where>
		order by gno desc))
		where rNum &gt;= #{startrow} and rNum &lt;=
		#{endrow}
		<!-- &gt;은 ~보다 크다(>), &lt;은 ~보다 작다(<) -->
	</select>


	<!-- 관리자 공지 내용+수정폼 -->
	<select id="gongji_cont" resultType="GongjiVO">
		select * from shop_gongji where gno=#{no}
	</select>


	<!-- 관리자 공지 수정 -->
	<update id="gongji_edit">
		update shop_gongji set gname=#{gname},
		gtitle=#{gtitle},gcont=#{gcont}
		where gno=#{gno}
	</update>

	<!-- 관리자 공지삭제 -->
	<delete id="gongji_del">
		delete from shop_gongji where gno=#{no}
	</delete>


</mapper>